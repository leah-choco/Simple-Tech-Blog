/*const User = require('./User');
const newName = require('./newName');

User.hasMany(newName, {
    foreignKey: "user_id",
    onDelete: 'CASCADE'
});

//newname.belongsTo(User, {
    foreignKey: 'user_id'
});

module.exports = { User, newName };*/